# TripTab 極端情境 QA 測試報告

- 測試日期：2026-07-28
- 測試版本：`aa0b114cec849fc22660edca8d4852311f5cecfa`
- 修正後回歸基準：`origin/main c8d43ab` 加上本次安全修正
- 測試環境：Windows、Node.js 24.18.0、npm 11.16.0、Chromium／Playwright CLI
- 測試站點：本機 `http://localhost:3000`
- 測試範圍：帳務、多國幣別、匯率、權限、邀請、結算、還款、管理者中心、API 錯誤處理、併發、響應式與效能煙霧測試

## 一、結論

目前版本的核心帳務零和、多國幣別換算、權限隔離、還款撤銷與主要 UI 流程整體穩定，但測得 2 個會影響帳務正確性的高優先問題，因此上線建議為：

> **修正後結論：本報告發現的 P1／P2／P3 程式缺陷均已修正並通過回歸；供應鏈掃描與正式環境容量壓測仍屬上線前待辦。**

本輪共整理出：

| 等級 | 數量 | 說明 |
|---|---:|---|
| P1 高 | 2 | 可能造成帳務重複或最小單位誤差 |
| P2 中 | 3 | API 5xx、錯誤分類與瀏覽器安全防線 |
| P3 低 | 3 | 型別邊界與 Console 雜訊 |
| 容量風險 | 1 | 高併發讀取下延遲明顯增加 |

沒有發現：

- 帳務不平衡或一般金額下的分攤誤差
- 權限繞過
- 換幣重複執行
- 匯率服務故障導致一般記帳不可用
- 還款重複寫入
- 可利用的前端 XSS
- 390／768／1024／1440px 水平破版

## 二、測試量與結果

不同矩陣之間有部分功能重疊，以下數字不直接相加作為「唯一案例總數」。

| 測試層級 | 執行量 | 結果 |
|---|---:|---|
| 單元測試 | 84 | 84 通過 |
| 資料庫整合套件 | 1 套完整情境 | `ALL_DATABASE_SCENARIOS_OK` |
| 帳務 Property／Fuzz，seed `0x51f15e` | 64,805 次檢查 | 全部通過 |
| 帳務 Property／Fuzz，seed `0x5eed1234` | 36,593 次檢查 | 全部通過；預期阻擋案例正確拒絕 |
| 財務 API／DB 隔離矩陣 | 86 | 84 通過，2 項揭露缺陷 |
| API／權限／韌性隔離矩陣 | 89 | 81 通過，8 個失敗案例歸納為 5 個根因 |
| 真實瀏覽器 UI 測試 | 44 | 44 通過 |
| 匯率韌性專測 | 14 | 14 通過 |
| 讀取併發煙霧測試 | 586 次請求 | 0 錯誤，但延遲隨併發升高 |
| 正式版 Build | 1 | 成功 |

## 三、缺陷清單

### P1｜API-IDEMP-001：新增支出缺少冪等保護

- 影響：手機網路重試、使用者雙擊或前端逾時重送時，可能建立兩筆完全相同的支出，直接改變結餘與結算。
- 重現方式：
  1. 以同一位使用者對同一群組準備相同支出 payload。
  2. 使用 `Promise.all` 同時送出兩次 `POST /api/groups/:id/expenses`。
  3. 兩次回應皆為 `201`。
  4. 再讀取群組，會看到兩筆相同支出。
- 預期：只有一次建立成功；相同冪等鍵的重送應回傳首次結果。
- 實際：`[201, 201]`，資料庫產生 2 筆。
- 程式位置：`server.js:1387-1417`，核心寫入在 `server.js:1406`。
- 建議：
  - API 接受 `Idempotency-Key`。
  - 以 `group_id + actor_id + key` 建立唯一約束或冪等結果表。
  - 前端提交中 Disabled 僅作輔助，不能取代後端冪等。

### P1｜FIN-RANGE-001：群組累計超過 JavaScript 安全整數後少 1 個最小單位

- 影響：極端大額帳本可能產生 1 cent 的結算誤差。
- 重現方式：
  1. 建立 USD 群組。
  2. 新增 3 筆各 `US$ 90,071,992,547,409.91` 的合法單筆支出。
  3. 三筆皆由 A 付款，唯一參與者為 B。
- 預期結算：`27,021,597,764,222,973` cents。
- 實際結算：`27,021,597,764,222,972` cents。
- 差異：`-1` cent。
- 根因：PostgreSQL `BIGINT`／`SUM` 字串在彙總後轉為不安全的 JavaScript `Number`。
- 程式位置：`server.js:946`、`server.js:951`、`server.js:1087`。
- 建議：
  - 帳本加總、餘額與結算全程使用 `BigInt` 或十進位字串。
  - 若短期無法調整 API 協議，至少限制「群組累計絕對值」不得超過 `Number.MAX_SAFE_INTEGER`，並回傳 `422`。

### P2｜API-ID-001：非法群組 UUID 造成 500

- 實際結果：
  - `GET /api/groups/not-a-uuid` → `500`
  - `POST /api/groups/not-a-uuid/expenses` → `500`
  - `DELETE /api/groups/not-a-uuid` → `500`
- 資料庫錯誤：PostgreSQL `22P02 invalid input syntax for type uuid`。
- 影響：
  - 使用者輸入錯誤被誤判為服務故障。
  - 產生無意義 5xx 日誌與監控告警。
- 程式位置：`server.js:914`、`server.js:1055-1056`、`server.js:1371`、`server.js:1391`。
- 建議：所有 UUID 路由共用驗證 middleware，進入資料庫前回傳 `400`。

### P2｜API-ERROR-001：Body parser 的 400／413 被轉成 500

- 超過 64KB JSON：
  - 預期：`413 Payload Too Large`
  - 實際：`500`
- 不完整或非法 JSON：
  - 預期：`400 Bad Request`
  - 實際：`500`
- 正向結果：回應未洩漏 stack trace，服務也未崩潰。
- 程式位置：Body limit 在 `server.js:51`，統一錯誤處理在 `server.js:1753`。
- 建議：錯誤 middleware 保留可信任的 `err.status`／`err.statusCode`，並為 4xx 回傳安全制式訊息。

### P2｜WEB-CSP-001：App shell 缺少 Content Security Policy

- 證據：Helmet 明確設定 `contentSecurityPolicy: false`，`GET /app` 沒有 `Content-Security-Policy`。
- 程式位置：`server.js:50`。
- 正向結果：
  - HTML-like 與 `<img onerror>` 測試標題均被 React 當純文字呈現。
  - 本輪沒有證實可利用的 XSS。
- 風險：未來若出現 HTML sink 或第三方腳本問題，缺少 CSP 會放大影響。
- 建議：先以 `Content-Security-Policy-Report-Only` 觀察，再逐步啟用正式 CSP。

### P3｜API-TYPE-001：群組名稱接受 Object

- 輸入：`{"name":{"value":"QA_API_OBJECT"}}`
- 實際：回傳 `201`，群組名稱成為 `[object Object]`。
- 程式位置：`server.js:866`。
- 影響：可能產生無意義資料，也代表 API 邊界存在型別混淆。
- 建議：名稱與說明先檢查 `typeof value === "string"`，拒絕物件與陣列。

### P3｜WEB-PRELOAD-001：登入後持續出現未使用 preload 警告

- 重複警告資源：
  - `hero-fuji-sakura.png`
  - `hero-airplane-watercolor.png`
- 現象：登入操作頁後，每次 reload／rerender 仍累積「preloaded but not used」警告。
- 影響：增加 Console 雜訊，也可能浪費首頁之外頁面的網路優先級。
- 建議：只在 Landing Page 路由 preload，或移除不必要 preload。

### P3｜WEB-AUTH-NOISE-001：未登入 `/api/me` 的預期 401 顯示為 Console error

- 現象：未登入開啟 `/app` 時，登入判斷正常，但 `/api/me` 的 `401` 會在 Console 顯示 Failed to load resource。
- 影響：不影響功能，但會增加監控與測試雜訊。
- 建議：將「尚未登入」視為預期狀態，避免以錯誤等級記錄。

## 四、帳務與多國幣別測試

### 已通過

- 支援幣別：TWD、JPY、KRW、USD、CNY、THB。
- 0 位小數幣別拒絕小數；2 位小數幣別拒絕三位小數。
- 零元、超過安全單筆金額與無效格式正確拒絕。
- 單一參與者負擔 100%，不會分給未選成員。
- Equal：
  - `US$ 10.01` 兩人分為 `US$ 5.00`／`US$ 5.01`。
- Exact：
  - `US$ 20.00` 指定為 `US$ 7.25`／`US$ 12.75`。
- Hybrid：
  - 固定 `US$ 5.00`，其餘 `US$ 25.00` 再均分。
- Weights：
  - 1:3 比例正確分配。
- 多人付款、退款負數方向、指定金額、比例、尾差皆維持付款總和＝分攤總和。
- 隨機餘額經最少轉帳演算法後全部歸零，收款人不需反向付款。
- 還款兩路併發只寫入一次；另一個請求正確拒絕。
- 有效與已撤銷還款換幣後保留方向及原回報金額。
- TWD → JPY → USD → TWD 全程維持零和。
- 同一換幣 Token 8 路並發：1 次執行、7 次安全重播。
- 舊幣別頁面在群組換幣後送出資料，正確回 `409`。
- Preview 後帳本版本改變、Token 過期、篡改、跨使用者使用皆正確拒絕。
- 換幣與新增支出競態只允許一方提交，另一方回 `409`。

### 正確阻擋的極端案例

- 權重過度懸殊，導致部分成員拿不到一個最小單位。
- 換幣後總金額小於參與人數需要的最小單位。
- JPY `100.5`。
- USD `10.001`。
- 金額超過安全單筆處理範圍。

## 五、API、權限與韌性測試

### 已通過

- 未登入、偽造或竄改 Session：`401`。
- 一般使用者呼叫管理者 API：`403`。
- 非成員讀群組、記帳、回報轉帳、讀銀行資料：`403`。
- 非建立者刪除群組、換幣、代他人回報：`403`。
- 建立者可依法修改成員建立的支出。
- 邀請 Token 無效時拒絕；重複加入維持冪等。
- 非法金額、空分攤、非群組付款人、重複付款人正確拒絕。
- 20 筆不同支出併發建立全部成功，帳務加總仍為零。
- 銀行帳戶 grant 只對指定付款人有效；其他成員不可重用。
- 刪除銀行帳戶後，既有 grant 立即失效。
- 錯誤轉帳金額、非建立者代報、重複回報正確拒絕。
- 付款人可撤銷自己的回報；無關成員不可撤銷；重複撤銷回 `409`。
- 匯率資料：
  - 完全缺漏／部分缺漏：換幣停用，一般記帳可用。
  - 超過 7 天：換幣停用。
  - 72 小時至 7 天：顯示警告但仍可預覽。
  - 主要來源失敗：切換 Cloudflare 鏡像。
  - 兩個端點逾時：正確記錄失敗與重試。
  - 無效或未來日期：拒絕同步。
  - 5 分、30 分、2 小時重試排程正確。
  - 多副本 advisory lock 正常。
- 群組刪除後，換幣快照與稽核 metadata 仍保留。
- 雙實例同時啟動時 migration 只執行一次。
- 重啟後 USD `3.33 / 3.33 / 3.34` 不會被舊 TWD migration 改寫。
- Header：
  - `X-Powered-By` 已移除。
  - `nosniff` 與 `SAMEORIGIN` 正常。

## 六、真實瀏覽器 UI／UX 測試

### 已通過

- 未登入頁、本機開發登入與自訂測試使用者登入。
- 建立 USD／JPY 群組、群組切換與幣別標示。
- 邀請第二位成員加入。
- 所有成員頭像顯示、個人資訊 Modal、Escape 關閉與焦點返回。
- 群組管理可以展開、再次點擊收合。
- 建立群組時，純空白名稱不允許提交。
- 空表單提交會標示欄位 invalid 並顯示錯誤。
- JPY 小數與超過安全整數的金額會顯示明確錯誤。
- HTML-like 支出名稱以純文字顯示，沒有執行。
- 四種分攤、修改支出、搜尋與金額升降冪排序。
- 「我已轉帳」、通知文字、LINE 分享網址、還款紀錄與撤銷回報。
- USD → JPY 換算預覽：
  - 匯率
  - 日期
  - 來源
  - 精度
  - 影響筆數
  - 尾差
  - 不可逆警告
  - 強制確認勾選
- 管理者中心四個頁籤、使用者搜尋、群組清單、稽核紀錄與分頁資訊。
- 新增支出 Modal 可用 Escape 關閉。
- 390px Modal 可在可視區域內垂直捲動。
- Authenticated 操作狀態最終 Console errors：0、Page errors：0。

### 響應式結果

| Viewport | body `scrollWidth`／`clientWidth` | 水平溢位 | 結果 |
|---:|---:|---:|---|
| 390px | 390／390 | 0px | 通過 |
| 768px | 768／768 | 0px | 通過 |
| 1024px | 1024／1024 | 0px | 通過 |
| 1440px | 1440／1440 | 0px | 通過 |

另一組扣除瀏覽器 scrollbar 的量測也得到 375／753／1009／1425px，水平溢位皆為 0。

390px 新增支出 Modal：

- 可視高度：約 775px。
- 內容高度：約 1,379px。
- 垂直滾動：正常。
- 水平溢位：0px。

### 未完整覆蓋

- 日期排序與參與金額排序未在真實瀏覽器逐一點擊；相關排序單元測試已通過。
- 單筆支出刪除未在本輪 UI 操作；API 與確認 Modal 測試已通過。
- 管理者中心手機版未於本輪重新測試。
- 未注入慢速網路或離線狀態。
- 未逐一對所有按鈕做 hover 視覺比對。

## 七、效能煙霧測試

此數據來自本機 Web 服務連線既有資料庫，不等同正式壓測，也沒有既定 SLA；只作容量風險判讀。

### 讀取結果

| 情境 | 請求數 | 錯誤 | p50 | p95 | 最慢 |
|---|---:|---:|---:|---:|---:|
| `/api/health` 同時讀取 | 200 | 0 | 2.53s | 2.94s | 2.99s |
| `/api/currencies` 同時讀取 | 200 | 0 | 1.92s | 2.56s | 2.62s |
| 群組帳本同時讀取 | 10 | 0 | 0.90s | 1.09s | 1.09s |
| 群組帳本同時讀取 | 25 | 0 | 2.01s | 2.38s | 2.41s |
| 群組帳本同時讀取 | 50 | 0 | 3.46s | 4.18s | 4.25s |
| 群組帳本同時讀取 | 100 | 0 | 9.44s | 11.03s | 11.17s |

### 判讀

- 正向：586 次讀取全部成功，沒有 timeout 或 5xx。
- 風險：群組帳本延遲隨併發接近線性上升。
- 建議：
  - 在與正式部署相同區域、相同資料庫規格下執行 k6／Artillery 壓測。
  - 設定 p95 SLA，再決定是否需要合併 SQL、快取群組摘要或調整連線池。
  - 檢查 `GET /api/groups/:id` 的多個資料庫查詢是否可合併或平行化。

## 八、建置、供應鏈與可觀測性

- `npm.cmd run build`：成功。
- Vite 警告：
  - JS bundle：625.35 kB，gzip 179.69 kB。
  - 超過預設 500 kB 提示。
- 專案使用既有 `pnpm-lock.yaml`。
- `pnpm audit --prod`：目前沒有已知 production dependency 漏洞。
- 建議：
  - 在 CI 持續執行 `pnpm audit --prod` 或等效相依性掃描。
  - 以 route-level dynamic import 拆分管理者中心與非首屏功能。

## 九、上線前修正順序

1. **P1：新增支出的 Idempotency-Key 與唯一約束。**
2. **P1：帳本累計與結算改用 BigInt／decimal string。**
3. P2：UUID middleware 與 parser 錯誤正確映射為 4xx。
4. P2：CSP 先以 Report-Only 導入。
5. P3：API 型別驗證與前端 Console 雜訊。
6. 建立 lockfile，完成依賴弱點掃描。
7. 在正式等級環境執行容量壓測。

## 十、建議回歸測試

修正 P1 後至少重新執行：

- 同一支出 2、8、20 路重送，只能新增一次。
- Idempotency-Key 跨群組、跨使用者不可互相污染。
- 超大群組累計、退款、還款與換幣後仍保持精確零和。
- 一般金額 TWD／JPY／KRW／USD／CNY／THB 全套回歸。
- `npm.cmd test`
- `npm.cmd run test:integration`
- 390／768／1024／1440px 主要流程。

## 十一、測試證據

### 修正後回歸結果

| 原始發現 | 修正內容 | 驗證結果 |
|---|---|---|
| 重複送出可能建立兩筆支出 | 新增 `Idempotency-Key`、請求指紋、資料庫唯一約束與同交易保存 | 相同請求併發只建立一筆；相同鍵搭配不同內容回傳 `409` |
| PostgreSQL `bigint` 轉成 JavaScript `Number` 可能失真 | 金額先以十進位字串／`BigInt` 驗證，超過安全範圍以 `422` 拒絕並回滾 | 邊界與超限整合測試通過，退款不會抵銷絕對值總量檢查 |
| 無效 UUID 導致資料庫 `500` | 所有群組、支出與結算識別碼先經 UUID middleware 驗證 | 無效識別碼穩定回傳 `400` |
| 錯誤 JSON 與過大 request body 被誤判為 `500` | 新增 parser 錯誤映射及一致錯誤代碼 | 分別回傳 `400 INVALID_JSON` 與 `413 REQUEST_BODY_TOO_LARGE` |
| 建立群組接受物件型別並寫入 `[object Object]` | 對名稱、說明與幣別加入明確字串型別驗證 | 非字串輸入回傳 `400` |
| App shell 沒有 CSP | 啟用 Helmet CSP 並限制 script、object、frame 與資源來源 | `/app` 回應包含 CSP；瀏覽器無 CSP 錯誤 |
| App 路由誤預載 Landing Hero 資源 | 移除全域 Hero preload，改由首頁元件按需載入 | `/app` 不再預載；首頁背景與飛機仍正常顯示 |
| 支出刪除後重送舊冪等鍵會回傳不存在的 ID | 保留刪除墓碑並回傳 `410`，冪等紀錄於 30 天後清理 | 不會復活支出，也不會回傳幽靈 ID |
| 極深 JSON 可能讓遞迴指紋計算耗盡 call stack | 在計算指紋前以非遞迴方式限制最大 32 層 | 過深內容穩定回傳 `400 IDEMPOTENCY_PAYLOAD_TOO_COMPLEX` |

修正後執行結果：

- 單元測試：`106／106` 通過。
- API 邊界與併發整合測試：`12／12` 通過。
- 完整資料庫情境：`ALL_DATABASE_SCENARIOS_OK`。
- Production build：成功。
- 真實瀏覽器：操作頁與新增共同支出 Modal 正常，Console 無錯誤或警告。
- 保留未登入 `/api/me` 的標準 `401` 語意；這是正確的驗證邊界，不改成成功狀態來隱藏瀏覽器網路紀錄。

- [1440px 操作頁](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-35-03-966Z.png)
- [1024px 操作頁](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-35-07-914Z.png)
- [768px 操作頁](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-35-42-557Z.png)
- [390px 操作頁](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-35-46-330Z.png)
- [390px 新增支出 Modal](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-36-42-493Z.png)
- [幣別選擇 Modal](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-34-10-696Z.png)
- [USD → JPY 換算預覽](output/playwright/qa-multicurrency/.playwright-cli/page-2026-07-28T05-34-38-058Z.png)

## 十二、資料清理

- `QA_FIN_` 測試群組、使用者、換幣紀錄與稽核紀錄：0 殘留。
- `QA_API_` 隔離 schema：已刪除。
- `QA_UI_`／`QA_ROOT_` 群組與使用者：0 殘留。
- QA 測試服務 3317／3318：已停止。
- Playwright QA sessions：已關閉。
- 主站 `localhost:3000`：測試後仍正常回應。
- 修正階段已修改產品程式碼與新增回歸測試；目前尚未建立 commit 或推送遠端。
